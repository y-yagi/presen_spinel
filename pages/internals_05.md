# 型推論

* Spinelにはrbsが渡せるようになっており、型推論に使用出来るようになっている
* rbsを渡しても推論は独立して動くようになっており、rbsに完全依存することはない
  * rbsはあくまでヒントで、推論がその上から走って矛盾があれば使用されない(型強制は出来ない)
* `--emit-rbs` で推論結果をrbsとして書き出せる[機能](https://github.com/matz/spinel/pull/1276)もある
