# 型推論

* プログラム全体(全ファイル)を一度にまとめて解析する
  * `require_relative` はパース時にインライン展開されるので、全ファイルが最初から見えている
* 呼び出し側の引数型を元にして、メソッド定義側のパラメータを決定する
  * 「このメソッドは実際にIntegerでしか呼ばれていない」という事実を使って型を絞り込む
* 型が制限出来ない場合、どんな型でも入れる「タグ付きunion」(`sp_RbVal`)を使用する
