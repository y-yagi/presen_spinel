# 対応しているRubyの機能

* ブロック: `yield`・`block_given?`・`&block`・`proc {}`・`lambda`・`method(:name)`
* 例外: `begin`/`rescue`/`ensure`/`retry`・`raise`(自作例外クラスも)
* 型: Integer・Float・String・Array・Hash・Range・Time・File・Regexp・Bigint・Fiber
* ロード：`require`、`require_relative`
