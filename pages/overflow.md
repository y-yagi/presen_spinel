# 整数オーバーフローの扱い

* ネイティブの整数は固定幅。あふれた時の挙動を`--int-overflow`で選べる
  * `raise`(既定): 例外を出す。安全側
  * `wrap`: 2の補数で黙って回す。最速
  * `promote`: Bigintに昇格してCRubyと同じ結果に
* `x = x * y` のようなオーバーフローしがちなループは自動でbigintにpromoteもされる
